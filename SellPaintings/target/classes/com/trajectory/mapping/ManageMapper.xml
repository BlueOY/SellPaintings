<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trajectory.dao.IShopDao">

	<insert id="insertPaintings" parameterType="com.trajectory.pojo.Painting" >
		INSERT INTO paintings (title,descript,author,paintingTime,uploadingTime,originalPrice,discountPrice,imageUrl) 
		VALUES (#{title},#{descript},#{author},#{paintingTime},now(),#{originalPrice},#{discountPrice},#{imageUrl})
	</insert>

</mapper>